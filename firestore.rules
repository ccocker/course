rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

     match /{document=**} {
      // Allow read, write, and delete operations only if the user is authenticated
      allow read, write, delete: if request.auth != null;
    }
  }
}

